
"use client";

import SessionCheckIn from "@/app/session-check-in/page";

/**
 * @fileOverview Phase: Before.
 * Directly maps to the Session Check-in flow for high-fidelity preparation.
 */
export default function BeforePhase() {
  return <SessionCheckIn />;
}
